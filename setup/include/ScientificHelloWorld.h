#include <string>
#include <math.h>

class ScientificHelloWorld{
	public:	
		double arg,answer;
		std::string message;
		ScientificHelloWorld(std::string phrase, double argument);
		void printMessage();
};
